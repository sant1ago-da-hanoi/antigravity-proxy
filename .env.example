PORT=3000
BASE_URL=http://localhost:3000

# Google OAuth client credentials
# Default: Google's Antigravity IDE installed-app OAuth client (extracted from Electron bundle).
# Project number 1071006060591 belongs to Google, not a third party.
# Per Google's docs, installed-app client secrets are public (embedded in binaries).
# See: https://developers.google.com/identity/protocols/oauth2#installed
# You can override with your own client if needed, but Claude/GPT models
# are only accessible through this specific client.
OAUTH_CLIENT_ID=1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com
OAUTH_CLIENT_SECRET=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

SAFETY_THRESHOLD=BLOCK_NONE

# Proxy access control (leave PROXY_PASSWORD empty to disable auth)
PROXY_USERNAME=admin
PROXY_PASSWORD=
PROXY_API_KEY=
